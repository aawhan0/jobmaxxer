# Contributing

## Local checks

```powershell
python -m pip install -r requirements.txt
python -m pytest -q
```

Keep provider parsing deterministic and test it with offline fixtures. Do not add credentials, automated applications, or unsolicited outreach behavior. Changes should preserve the local-first workflow and avoid network access in unit tests.
